# Merci Μαγειρευτό — online menu

Στατικό site με το ημερήσιο μενού ενός μαγαζιού σπιτικού φαγητού στη Λάρισα.
Δημοσιεύεται στο GitHub Pages: <https://stivakos.github.io/magirefto-menu/>
Ο πελάτης το φτάνει κυρίως **σκανάροντας QR** μέσα στο μαγαζί.

Όλο το περιεχόμενο προς τον χρήστη είναι **στα ελληνικά** — μηνύματα, σχόλια,
commit messages.

## Κανόνες που δεν σπάνε

1. **Το `index.html` και το `ΠΙΑΤΑ.md` είναι ΠΑΡΑΓΟΜΕΝΑ.** Ποτέ μην τα
   επεξεργάζεσαι με το χέρι — το επόμενο build τα ξαναγράφει. Αλλαγές
   εμφάνισης γίνονται στο `build-source/build.py`.
2. **Το `DAILY_MENU.xlsx` το συντηρεί ο ιδιοκτήτης.** Το build το διαβάζει,
   ποτέ δεν το γράφει.
3. **Τιμές: ρώτα πριν γράψεις.** Δες «Πηγές τιμών» πιο κάτω.
4. **Ποτέ μην αλλάζεις υπάρχοντα Α/Α.** Δείχνει σε αυτά το `menu-today.txt`·
   η αλλαγή βγάζει σιωπηλά λάθος μενού. Νέα πιάτα παίρνουν το επόμενο
   ελεύθερο Α/Α στο τέλος του tab.

## Ροή

```
menu-today.txt   (τι σερβίρεται σήμερα — αριθμοί Α/Α)
DAILY_MENU.xlsx  (η βάση: πιάτα, τιμές)
        │
        ├─→ check.py   επικύρωση· exit 1 => σταματά η δημοσίευση
        └─→ build.py   ─→ index.html  +  ΠΙΑΤΑ.md
                              │
                       GitHub Action ─→ commit ─→ Pages
```

## Εντολές

```bash
cd build-source
python3 check.py     # ΠΑΝΤΑ πριν το commit· δείχνει τι θα δει ο πελάτης
python3 build.py     # τοπικό preview -> ../index.html
```

Απαιτεί `openpyxl`. Το `check.py` διαβάζει τις κατηγορίες από το `build.py`
μέσω AST — αν αλλάξεις το `CATEGORIES`, ο έλεγχος ακολουθεί μόνος του.

## Αρχεία

| Αρχείο | Ρόλος |
|---|---|
| `menu-today.txt` | Η επιλογή της ημέρας. Ο ιδιοκτήτης το αλλάζει **από κινητό** μέσω GitHub. |
| `DAILY_MENU.xlsx` | Βάση πιάτων. Ένα tab ανά κατηγορία. |
| `build-source/build.py` | Ο generator. Όλα inline (CSS, fonts, SVG, JS) — μία αυτοτελής σελίδα, εκτός από τις φωτό της γκαλερί. |
| `build-source/check.py` | Επικύρωση πριν το build. |
| `build-source/fonts.css` | Vendored GFS Didot, για να τρέχει το build και σε CI. |
| `build-source/social.py` | Εικόνα μαυροπίνακα για FB/Instagram. Παράγεται κατ' απαίτηση. |
| `build-source/prep-photos.sh` | Σμικρύνει τις φωτό της γκαλερί. Τρέχει χειροκίνητα, όχι σε CI. |
| `build-source/prep-canvas.py` | Φτιάχνει τον καμβά του μαυροπίνακα. Χειροκίνητα, όχι σε CI. |
| `assets/gallery/` | Οι 6 φωτογραφίες της ενότητας «Το μαγαζί μας». Παραγόμενες. |
| `assets/qr-menu.svg` `.png` | Το τυπωμένο QR του μαγαζιού. Δείχνει στη **ρίζα** του site. |
| `ΠΙΑΤΑ.md` | Ευρετήριο «ποιος αριθμός = ποιο πιάτο», κατά συχνότητα. Παραγόμενο. |
| `index.html` | Το site. Παραγόμενο. |

## `menu-today.txt`

```
ΚΛΕΙΣΤΑ: Τρίτη 25/8          ← αν υπάρχει, το μαγαζί είναι ΚΛΕΙΣΤΟ
ΗΜΕΡΟΜΗΝΙΑ: Τρίτη 25/8/26
Μενού Ημέρας: μπιφτέκι, μουσακάς, γεμιστά     ← ονόματα (με κόμμα)
Συνοδευτικά: 1 2 4 5                          ← ή αριθμοί (με κενά)
```

### Ονόματα αντί για αριθμούς

Ο ιδιοκτήτης δίνει το μενού **περιγραφικά** («μπιφτέκι, φιλέτο κοτόπουλο,
μουσακάς»), όχι με Α/Α. Το `menu-today.txt` δέχεται και τα δύο, ακόμη και
ανακατεμένα (`38, μουσακάς, 8`). Η αναγνώριση ζει στο **`dish_names.py`**, που το
χρησιμοποιούν **και** το `build.py` **και** το `check.py` — αν αποκλίνουν, ο
έλεγχος θα ενέκρινε άλλο μενού από αυτό που θα χτιστεί.

Αγνοεί τόνους και ορθογραφία (`ρεβιθια` → «Ρεβύθια») και δέχεται μερικό όνομα
(`μπιφτέκι` → «Μπιφτέκι κοτόπουλο»). **Ποτέ δεν μαντεύει:**

| Είσοδος | Αποτέλεσμα |
|---|---|
| `γεμιστά` | #8 «Γεμιστά» — το ακριβές όνομα κερδίζει το «Γεμιστά με κιμά» |
| `φιλέτο κοτόπουλο` | **σφάλμα** — #40 σχάρας ή #54 a la creme; |
| `πίτσα` | **σφάλμα** — δεν υπάρχει· προτείνει ό,τι μοιάζει |

Ασάφεια ή άγνωστο όνομα **σταματά και τον έλεγχο και το build** (exit 1). Το να
παραλειφθεί σιωπηλά ένα πιάτο θα έβγαζε λάθος κατάλογο χωρίς να το πάρει είδηση
κανείς — χειρότερο από το να μη δημοσιευτεί καθόλου.

Όσο υπάρχει γραμμή `ΚΛΕΙΣΤΑ`, το build βγάζει σελίδα-ανακοίνωση «Ανοίγουμε …»
και **παραλείπει** μενού, chips, μπάρα παραγγελίας και `ORDER_JS` — δεν
κρύβονται με CSS, δεν υπάρχουν στο markup. Για άνοιγμα: σβήσε τη γραμμή.

Κατηγορία χωρίς αριθμούς δεν εμφανίζεται. Η σειρά εμφάνισης = η σειρά που
γράφονται οι αριθμοί, όχι η σειρά στο xlsx.

## `DAILY_MENU.xlsx`

Γραμμή 1 = κεφαλίδες, δεδομένα από τη 2. **Στήλες A=Α/Α, B=Ονομασία, C=Τιμή**
σε σταθερές θέσεις. Το `build.py` διαβάζει A–C.

**Στήλη E «Με συνοδευτικό;»** (tab `Μενού Ημέρας`) — `Ν` = το πιάτο σερβίρεται με
ένα συνοδευτικό **χωρίς επιπλέον χρέωση**. Κενό ή `όχι` = δεν παίρνει. Στο site,
όταν ο πελάτης βάλει το πιάτο στο καλάθι, εμφανίζεται **ένα select ανά μερίδα**
(2 μπιφτέκια → «Μερίδα 1», «Μερίδα 2»: μπορούν να πάρουν διαφορετικό συνοδευτικό)
και η επιλογή μπαίνει στο SMS ως `↳ με Πατάτες τηγανητές`.

Οι επιλογές είναι τα **συνοδευτικά της ημέρας** από το `menu-today.txt`, συν
«Χωρίς συνοδευτικό». Η παραγγελία δεν στέλνεται αν μείνει κάποιο αδιάλεκτο.

Αν η στήλη **δεν υπάρχει** (παλιό xlsx), όλα βγαίνουν `False` και η λειτουργία
μένει αδρανής — το site δουλεύει ακριβώς όπως πριν. Το `check.py` προειδοποιεί για
τιμές που δεν αναγνωρίζονται (θα αγνοούνταν σιωπηλά) και για σημαδεμένα πιάτα σε
μέρα χωρίς συνοδευτικά.

**Στήλη D «Συχνό;»** — `Κ` = σχεδόν κάθε μέρα, `Σ` = συχνά, κενό = σπάνια.
Χρησιμοποιείται **μόνο** για τη σειρά στο `ΠΙΑΤΑ.md`, ώστε ο ιδιοκτήτης να
βρίσκει γρήγορα τους αριθμούς που όντως χρησιμοποιεί. Δεν επηρεάζει το site.
Αρχικά συμπληρώθηκε από ανάλυση 107 φωτογραφιών μαυροπινάκων· η κρίση του
ιδιοκτήτη υπερισχύει.

Ονόματα tabs — **ακριβώς** αυτά, αλλιώς σκάει το build:
`Μενού Ημέρας` · `Συνοδευτικά` · `Σαλάτες` · `Γλυκά` · `Αναψυκτικά - Ποτά`

⚠ Το tab γράφεται με **παύλα** (`Αναψυκτικά - Ποτά`), το `menu-today.txt` με
**κάθετο** (`Αναψυκτικά / Ποτά`). Σωστό — μην τα «διορθώσεις».

Τα **Συνοδευτικά** εμφανίζονται πάντα χωρίς τιμές, με σταθερή σημείωση.

### Σιωπηλές παγίδες (γι' αυτό υπάρχει το check.py)

Τιμή γραμμένη ως **κείμενο με κόμμα** (`8,50`) δεν χαλάει μόνο την τιμή —
ρίχνει **ολόκληρη τη γραμμή** και το πιάτο εξαφανίζεται. Ίδιο για διπλό ή
μη-ακέραιο Α/Α, πιάτο χωρίς Α/Α, αριθμό στο `menu-today.txt` που δεν
αντιστοιχεί σε πιάτο. Το `check.py` τα πιάνει όλα, μαζί με ορθογραφικές
παραλλαγές του ίδιου πιάτου (`Ρεβίθια`/`Ρεβύθια`, `Γλώσσα`/`Γλώσσες`).

## CI

`.github/workflows/build-menu.yml` — τρέχει σε push που αγγίζει
`menu-today.txt`, `DAILY_MENU.xlsx` ή `build-source/**`.
Σειρά: `check.py` → `build.py` → commit `index.html` + `ΠΙΑΤΑ.md` με
`[skip ci]` (αλλιώς θα καλούσε τον εαυτό του).

Αν το `check.py` βγάλει σφάλμα, η δημοσίευση σταματά και το site μένει στην
τελευταία καλή έκδοση.

## Πηγές τιμών — έχουν αποκλίνει

- **`DAILY_MENU.xlsx` (εδώ)** = η αυθεντία για το site.
- **`~/Documents/ΜΑΓΕΙΡΕΥΤΟ/Μαγειρευτό_Μενού.xlsx`** = 320 πιάτα, στήλη «Τιμή
  Πλατφόρμας». Πιστή μεταγραφή του καταλόγου **efood**, άρα περιέχει
  προμήθεια πλατφόρμας ~28%.

Σε 90 κοινά πιάτα: 86 ίδια, 4 διαφορετικά (Παστίτσιο & Μουσακάς 8,50 → 7,00).
Οι αποκλίσεις **δεν** ακολουθούν σταθερό ποσοστό — τιμή λιανικής δεν
υπολογίζεται από την τιμή πλατφόρμας. Αν λείπει τιμή: πρότεινέ τη από το
master και **ζήτα επιβεβαίωση**· μην τη γράψεις σιωπηλά.

## Δεδομένα εκτός repo

`~/Documents/ΜΑΓΕΙΡΕΥΤΟ/` (437 MB, εκτός git): φωτογραφίες πιάτων, 108 φωτό
μαυροπινάκων, master κατάλογος, logos, QR. Κρατά **αντίγραφο** του
`DAILY_MENU.xlsx` — να μένει συγχρονισμένο, αλλά το repo είναι το αυθεντικό.

## Το μαγαζί

Καρδίτσης 22, 41335 Λάρισα · ☎ 2414010332 · κιν./Viber +30 698 799 2887
Δευτέρα–Σάββατο 12:00–16:00 · take away & delivery, παραγγελίες με SMS.
Νομικό πρόσωπο: Chrysas Paradiso IKE.

**Κλειστό για διακοπές — ανοίγει Τρίτη 25/8/2026.**

## Κατεύθυνση

Συμφωνημένο: **μία σελίδα**. Το μενού μένει στη ρίζα και ο εμπλουτισμός μπαίνει
**κάτω** από το μενού.

Το τυπωμένο QR (`assets/qr-menu.svg`) κωδικοποιεί
`https://stivakos.github.io/magirefto-menu/` — επαληθευμένο. Όσα χαρτιά είναι ήδη
τυπωμένα και κολλημένα στο μαγαζί δείχνουν εκεί: **η ρίζα δεν μετακινείται**.

**Έγινε:** η ενότητα «Το μαγαζί μας» — κείμενο γνωριμίας + γκαλερί 6
φωτογραφιών, με δικό της chip στο rail. Φαίνεται **και όταν το μαγαζί είναι
κλειστό** (κάτω από την ανακοίνωση): όσο είμαστε κλειστοί είναι το μόνο που
έχει να δει όποιος σκανάρει το QR. Ώρες, διεύθυνση και τηλέφωνα δεν
επαναλαμβάνονται εκεί — υπάρχουν ήδη στο footer.

**Επόμενο (όταν ανοίξει το μαγαζί):** ο ιδιοκτήτης θα φωτογραφίσει τα πιάτα
**ένα-ένα σε ντεκόρ** και θα μπουν **δίπλα σε κάθε περιγραφή**, με σχόλιο
τύπου «ό,τι βλέπετε, παίρνετε». Αυτό ακυρώνει τον παλιό κανόνα «όχι
φωτογραφία ανά πιάτο» — ίσχυε μόνο επειδή υπήρχαν 16 φωτό για 57 πιάτα και η
άνιση κάλυψη έδειχνε ημιτελής. Θα χρειαστεί αντιστοίχιση φωτό ↔ Α/Α (πιθανή
νέα στήλη στο `DAILY_MENU.xlsx`) και fallback για όσα πιάτα δεν έχουν ακόμη
φωτογραφία.

### Social — η εικόνα του μαυροπίνακα

```bash
python3 build-source/social.py     # -> social/menu.jpg (1080×1350, 4:5)
```

Παίρνει τα δεδομένα με **`import build`**: το `build.py` κρατά τα γραψίματά του
πίσω από `if __name__ == "__main__"`, οπότε το `social.py` διαβάζει `MENU`,
`MENU_DATE`, `CLOSED` χωρίς να ξαναγραφτεί το `index.html`. **Μία πηγή αλήθειας
— μην αντιγράψεις το parsing.** Αν σπάσει αυτός ο guard, το `social.py` θα
ξαναγράφει σιωπηλά το site.

Η σελίδα στήνεται σε HTML και φωτογραφίζεται με **headless Chrome**, ώστε να
χρησιμοποιεί το ίδιο `fonts.css` (GFS Didot) με το site. Το Pillow δεν θα
μπορούσε — οι ενσωματωμένες γραμματοσειρές είναι woff2. Δύο παγίδες:

- **Ποτέ `--user-data-dir` σε φρέσκο φάκελο**: ο Chrome μπαίνει σε first-run και
  κρεμάει επ' αόριστον αντί να βγάλει screenshot.
- Ο headless Chrome σε macOS έχει **ελάχιστο πλάτος παραθύρου ~500px** — μικρότερα
  screenshots βγαίνουν κομμένα, όχι σμικρυμένα. Δεν είναι bug της σελίδας.

Εμφανίζονται **Μενού Ημέρας** (μία στήλη), **Σαλάτες** και **Γλυκά** (δύο στήλες —
κοντά ονόματα, μισές γραμμές). Συνοδευτικά και Αναψυκτικά μένουν στη γραμμή «και
ακόμη». Όταν υπάρχει `ΚΛΕΙΣΤΑ`, βγαίνει ανακοίνωση αντί για μενού.

**Το μέγεθος γραμμάτων το αποφασίζει η ίδια η σελίδα.** Το `scale_for()` δίνει μια
πρώτη εκτίμηση και μετά JS στη σελίδα μετράει και μικραίνει μέχρι να χωρέσει
πραγματικά. Χρειάστηκαν τρεις προσπάθειες — οι δύο παγίδες αξίζουν προσοχή:

- `.board` **πρέπει** να έχει `min-height:0`. Τα flex items έχουν `min-height:auto`
  και δεν συρρικνώνονται κάτω από το περιεχόμενό τους, οπότε το `clientHeight`
  μεγάλωνε μαζί με τη λίστα και η σύγκριση έβγαινε πάντα ψευδής.
- Η μέτρηση γίνεται στο `.board-inner`, όχι στο `.board`: σε κεντραρισμένο flex η
  υπερχείλιση βγαίνει **και από πάνω** και το `scrollHeight` δεν τη δείχνει.

Δοκιμασμένο με 6, 12, 18 πιάτα και με το ακραίο 18+13+11 (χωράει, στα 15px).

**Η παραγόμενη εικόνα ΔΕΝ μπαίνει σε git** (`social/` στο `.gitignore`): το git
κρατά κάθε έκδοση, άρα ~90 MB τον χρόνο. Πού θα φιλοξενείται όταν αυτοματοποιηθεί
η δημοσίευση — το Instagram Graph API θέλει **δημόσιο URL** — αποφασίζεται μαζί με
τον τρόπο δημοσίευσης.

### Δημοσίευση στα social — με ρητή έγκριση

```
menu-today.txt αλλάζει
   └─ build-menu.yml: check → build → social.py
                       ├─ push εικόνας σε orphan branch «social-preview»
                       └─ publish.py --reset  (ΔΗΜΟΣΙΕΥΣΗ: όχι + νέα ημερομηνία)

ο ιδιοκτήτης βλέπει την εικόνα από το κινητό, γράφει ΔΗΜΟΣΙΕΥΣΗ: ναι
   └─ publish-social.yml: publish.py → Facebook + Instagram → σφράγισμα «έγινε»
```

**Γιατί δύο βήματα.** Στο GitHub από κινητό βλέπεις κείμενο, όχι εικόνα. Λάθος
αριθμός περνάει το `check.py` (ο Α/Α υπάρχει, απλώς δείχνει άλλο πιάτο) και στο
site διορθώνεται σε ένα λεπτό — αλλά **το Instagram δεν επιτρέπει αντικατάσταση
εικόνας σε δημοσιευμένο post**. Σβήσιμο και ξαναδημοσίευση χάνει σχόλια και likes.

Το `publish.py` ελέγχει τρία πράγματα, καθένα για συγκεκριμένο λάθος:

| Έλεγχος | Τι αποτρέπει |
|---|---|
| `ΔΗΜΟΣΙΕΥΣΗ: ναι` | σιωπηλή δημοσίευση χωρίς να το θέλεις |
| ημερομηνία `post.txt` == `menu-today.txt` | έγκριση εικόνας άλλης μέρας |
| δεν γράφει ήδη «έγινε» | δεύτερο commit = δεύτερο post την ίδια μέρα |

Χωρίς secrets κάνει **στεγνή δοκιμή** και βγαίνει με 0 — η ροή δοκιμάζεται
ολόκληρη χωρίς λογαριασμούς. ⚠ Οι κλήσεις στο Graph API είναι **αδοκίμαστες**
μέχρι να υπάρξουν tokens.

Η εικόνα ζει σε **orphan branch** (`social-preview`, force-push, πάντα ένα commit)
και όχι στο `main`: το git κρατά κάθε έκδοση και θα φούσκωνε ~75 MB τον χρόνο σε
repo που σήμερα είναι 3 MB. Το Instagram θέλει δημόσιο URL — γι' αυτό
`raw.githubusercontent.com`. Προσοχή: το raw έχει CDN cache λίγων λεπτών, οπότε η
προεπισκόπηση μπορεί να αργήσει λίγο να ανανεωθεί.

### Γκαλερί — πώς αλλάζουν οι φωτογραφίες

Οι εικόνες είναι **αρχεία** στο `assets/gallery/` — η **μόνη** εξαίρεση στο
«όλα inline». Λόγος: 6 φωτό σε base64 θα πρόσθεταν ~1 MB στο `index.html`,
που ο πελάτης κατεβάζει με 4G μέσα στο μαγαζί· ως αρχεία φορτώνουν lazy.

```bash
# 1. άλλαξε τη λίστα PHOTOS στο build-source/prep-photos.sh
./build-source/prep-photos.sh          # σμικρύνει σε 800px / q60 (~130 KB η μία)
# 2. ενημέρωσε το GALLERY στο build.py (όνομα, alt, object-position)
python3 build-source/check.py          # πιάνει λείπουσες & αχρησιμοποίητες
```

Το `prep-photos.sh` τρέχει **μόνο τοπικά** (θέλει `sips` και τον φάκελο
`~/Documents/ΜΑΓΕΙΡΕΥΤΟ/ΦΩΤΟ ΦΑΓΗΤΑ/`, εκτός git). Στο CI δεν τρέχει ποτέ —
τα παραγόμενα jpg γίνονται commit.
